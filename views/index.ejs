<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NoteNest || make your notes here</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-zinc-800">
    <nav class="bg-gray-800 py-4">
        <div class="mx-auto flex justify-between items-center px-4 ">
            <div>
                <a href="#" class="text-white text-2xl font-semibold">NoteNest</a>
            </div>
            <div class="navbar md md:flex gap-10 bg-gray-800">
                <a href="#" class="text-white text-xl hover:text-gray-300">Home</a>
                <a href="#" class="text-white text-xl hover:text-gray-300">About</a>
                <!-- <a href="#" class="text-white text-xl hover:text-gray-300">Services</a> -->
                <a href="#" class="text-white text-xl hover:text-gray-300">Contact</a>
            </div>
            <div class="md:hidden">
                <button class="text-white focus:outline-none btn z-50">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="h-6 w-6 fill-current">
                        <path fill-rule="evenodd" d="M4 5a1 1 0 1 1 0-2h16a1 1 0 1 1 0 2H4zm0 7a1 1 0 1 1 0-2h16a1 1 0 1 1 0 2H4zm0 7a1 1 0 1 1 0-2h16a1 1 0 1 1 0 2H4z"/>
                    </svg>
                </button>
            </div>
        </div>
    </nav>
    </div>
    <div class="px-10">
        <div class="mx-auto bg-zinc-700 p-4 rounded-md shadow-md mt-8">
            <h1 class="p-2 text-2xl">Create your Tasks here</h1>
            <form action="/create" method="POST">
                <div class="mt-2">
                    <input type="text" id="title" name="title" class=" bg-zinc-800 text-white w-full border-gray-300 rounded-md px-3 py-2 focus:outline-none focus:border-blue-500" placeholder="Enter title">
                </div>
                <div class="mt-2">
                    <textarea id="description" name="description" class=" bg-zinc-800 text-white w-full border-gray-300 rounded-md px-3 py-2 focus:outline-none focus:border-blue-500" placeholder="Enter description"></textarea>
                </div>
                <div>
                    <button type="submit" class="bg-blue-800 text-white px-4 py-2 rounded-md hover:bg-blue-600 focus:outline-none focus:bg-blue-600">Submit</button>
                </div>
            </form>
        </div>
    </div>
    <div class="tasks px-10 mt-5 flex gap-4 flex-wrap">
        <% if (files.length > 0) { %>
            <% files.forEach(function(val) { %>
                <div class="task w-72 px-3 py-4 bg-zinc-700 rounded-md">
                    <h1 class="text-white cursor-default"><%= val %></h1>
                    <div class="flex justify-between">
                        <a href="/file/<%=val %>" class="text-blue-600">Read more....</a>
                        <a href="/edit/<%=val %>" class="text-yellow-100">Edit</a>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <h1 class="text-white text-4xl">No tasks created Yet . . . .</h1>
        <% } %>
    </div>
    <script src="/javascripts/navbar_toogle.js"></script>
</body>
</html>
